meta {
  name: Update Message
  type: http
  seq: 16
}

patch {
  url: {{base_url}}/v1/ticket/{{ticket_id}}/messages/{{message_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "body": "Mensagem atualizada."
  }
}
