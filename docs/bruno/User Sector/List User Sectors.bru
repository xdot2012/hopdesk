meta {
  name: List User Sectors
  type: http
  seq: 1
}

get {
  url: {{base_url}}/v1/user_sector/
  body: none
  auth: inherit
}

script:post-response {
  let data = res.body;
  if (Array.isArray(data) && data.length > 0 && data[0].id) {
    bru.setEnvVar("user_sector_id", data[0].id);
  }
}
