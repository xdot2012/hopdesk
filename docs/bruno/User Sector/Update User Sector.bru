meta {
  name: Update User Sector
  type: http
  seq: 3
}

put {
  url: {{base_url}}/v1/user_sector/{{user_sector_id}}
  body: json
  auth: inherit
}

body:json {
  {
    "sectorId": "{{sector_id}}",
    "isSectorManager": false
  }
}
