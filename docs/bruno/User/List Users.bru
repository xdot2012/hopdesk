meta {
  name: List Users
  type: http
  seq: 5
}

get {
  url: {{base_url}}/v1/user/
  body: none
  auth: inherit
}

script:post-response {
  const data = res.getBody();
  if (Array.isArray(data) && data[0]?.id) {
    bru.setEnvVar("managed_user_id", data[0].id);
  }
}
